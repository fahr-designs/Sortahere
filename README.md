# Sortahere

A guest management and email invitation system for events.

## Development

1. Install dependencies: `npm install`

2. Create a `.env` file with `PORT`, `DB_PATH`, `ADMIN_TOKEN`, and `RESEND_API_KEY`

3. Run the development server: `npm start`
